# Pinned runtime dependency lock — this is what the Docker image installs
# (pip install -r requirements.txt). pyproject.toml [project.dependencies] holds
# the abstract version floors used for dev installs (pip install -e .[dev]); the
# two are kept consistent (every pin here satisfies the matching pyproject floor)
# and must be bumped together.
aiodns==3.1.1
aiohttp==3.13.3
brotli==1.2.0
dotmap==1.3.30
fastapi==0.135.1
httptools==0.6.4
Jinja2==3.1.6
pydantic==2.12.5
pydantic-settings==2.12.0
python-multipart==0.0.22
starlette==0.50.0
uvicorn==0.34.0
uvloop==0.21.0
xmltodict==0.14.1
