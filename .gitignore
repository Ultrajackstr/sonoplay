# ── Documentation & Tests (managed outside repo) ──
docs/
tests/

# ── Python artifacts ──
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg
*.egg-info/
dist/
build/
eggs/
*.manifest
*.spec

# ── Virtual environments ──
.venv/
venv/
ENV/
env/
.python-version

# ── Testing artifacts ──
.pytest_cache/
pytest_cache/
pytest.ini
.coverage
htmlcov/
.tox/
.nox/

# ── IDE / Editors ──
.idea/
.vscode/
*.swp
*.swo
*~

# ── OS files ──
.DS_Store
Thumbs.db

# ── Environment & secrets ──
.env
.env.local
*.pem
*.key
secrets/

# ── Runtime data (mounted as volumes) ──
config/
data/
temp/

# ── GitHub (track workflows only) ──
.github/
!.github/workflows/

# ── Git worktrees ──
.worktrees/

# ── Logs ──
*.log
logs/

# ── Docker overrides ──
docker-compose.override.yml

# ── Misc loose root files ──
start.sh
.git.bak/