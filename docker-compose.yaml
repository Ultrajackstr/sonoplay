services:
  sonoplex:
    build: .
    container_name: sonoplex
    network_mode: host
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    volumes:
      - sonoplex-config:/config

volumes:
  sonoplex-config: