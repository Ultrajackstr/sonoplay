{
  "name": "sonoplay-web-tests",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright browser tests for the SonoPlay web UI",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
